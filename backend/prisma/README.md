# Prisma

El schema principal del proyecto esta en:

```txt
../schema_silab_fci.prisma
```

Los scripts de `backend/package.json` apuntan a ese archivo para validar, formatear, generar cliente y ejecutar migraciones.

